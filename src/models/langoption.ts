export class LangOption {
  iso: string;
  name: string;
  iconPath: string;
  constructor(iso: string, name: string, iconPath: string) {
    this.iso = iso;
    this.name = name;
    this.iconPath = iconPath;
  }
}
